---
title: "Sales"
date: "`r Sys.Date()`"
output: html_document
---

```{r load-packages, message=FALSE}
library(tidyverse)
library(readxl)
```

* Read in the Excel file called `sales.xlsx` from the `data-raw/` folder 
such that it looks like the output in the slides.

```{r}

```

* **Stretch goal:** Manipulate the sales data such sich that it looks like the 
following.

```{r}

```

